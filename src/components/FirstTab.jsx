// import {useEffect} from "react";

const FirstTab = ({ clean }) => {

    // useEffect(() => {
    //     if (clean) {
    //
    //     }
    // }, []);

    return (
        <h1>Send Message tab</h1>
    );
};
export default FirstTab;